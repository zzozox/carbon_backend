<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carbon.system.mapper.FeishuFiletokenMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        code, file_name, file_token, file_url, app, web_redirect, create_time, update_time
    </sql>

    <select id="getDocToken" resultType="java.util.HashMap">
        select id,url,title,updated_time from carbon_article
    </select>

</mapper>
